# Environment Configuration
# Non-sensitive URLs are configured in wrangler.jsonc
# Only secrets need to be in .env.local

# Required secret (create .env.local with this):
VITE_SUPABASE_ANON_KEY=your-anon-key
